server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name 34-242-206-196.sslip.io;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 503;
    }
}
