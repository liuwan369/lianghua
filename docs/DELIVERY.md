# 交付与验收状态

截至 2026-09-11 核验，正式六页、行情、模拟账本、账户 V2 只读能力及受控真实下单/撤单已验证；部分成交、断线恢复和真实财务到账仍未完成，判定为 **NOT_READY_FOR_LIVE_TRADING**。

## 可复核证据

| 证据 | 观测 | 能证明什么 |
|---|---|---|
| [公网 API 样本](evidence/2026-09-10/public-api-final.json) | status/config/markets/runs/account 请求成功，约 0.95–1.05 秒；运行 false、paper、live_unlocked false | 当次只读接口可用，不是全部按钮与交易验收 |
| [账户授权范围](ACCOUNT-APPROVAL-SCOPE-2026-09-10.json) | 实际 V2 标准与 neg-risk allowance/operator 满足要求 | 当前交易路径所需授权，不包含其他产品全部授权 |
| [私有 CLOB 只读记录](evidence/2026-09-10/private-clob-readonly.json) | V2 原始版本 2，signature type 3，已有 API key 派生成功；订单首屏 0、成交首屏 19、余额 108.728676、4 项正 allowance | 经认证私有读取成功，不证明前端同步或真实下单 |
| [六分钟 paper 汇总](evidence/2026-09-10/paper-audit-summary.json) | 3 个市场窗口，其中一轮 UP 20 份、成本 4.60，oracle 推导 DOWN 获胜，模拟结算 -4.60 | 存在单边风险；不等于真实亏损，也缺少全程 tick 拒绝诊断来确定成因 |
| [tick paper 汇总](evidence/2026-09-10/paper-tick-final-summary.json) | 90 秒配置实际运行含启动 106.72 秒；两边 tick 0.01，模拟 DOWN 20 份成本 3.60，35 次 hedge_pair_cost 拒绝，正常退出 | tick 传递与诊断有效；停止时未结算，3.60 不是已实现亏损 |
| [受控实盘下单/撤单](evidence/2026-09-11/live-order-cancel-acceptance.json) | `$0.05` maker 单真实返回 `live`，ACK 148.536 ms，随后撤单确认；余额不变、开放订单 0 | 证明真实签名、下单 ACK、撤单 ACK 和残单清理；没有证明成交或部分成交 |
| [受控实盘成交/结算观察](evidence/2026-09-11/live-fill-settlement-observation.json) | 100 份 UP 真实成交，交易回报、链上哈希、手续费回执和余额变化均已读取；市场进入 `redeemable=true`；Relayer 赎回真实返回 401 且未广播 | 证明真实成交和费用读取；Deposit Wallet 结算仍需 Builder 认证，未把可赎回误记为已到账 |

私有只读记录生成时间为 `2026-09-10T06:40:52Z`，未保留精确请求时间或 HTTP 状态码，不能据此编造每个请求的 200 状态。记录未涉及新 key 创建、账户配置修改、余额更新、下单、撤单或链上交易。首屏条数不是完整交易历史。

2026-09-10 软件验证记录：Python **248 passed / 1 skipped**；引擎 **212 passed / 22 files**、TypeScript 构建通过；前端 **39 passed**、构建通过。测试覆盖模拟协议与风险路径，不能代替真实资金测试。

## 必须完成的验收

- 真实订单启停、下单和撤单已完成受控验证；部分成交、替换、断线恢复、在途/迟到回报和退出核对仍需真实场景验收。当前页面实盘入口仍禁用。
- 账户基础只读数据已接线；仍需扣除占用后的可花余额、完整逐委托生命周期及启动联合校验。
- 真实手续费和成交回执已读取；Deposit Wallet 结算、返佣、奖励到账凭证和钱包现金对账仍未完成。奖励规则与示例计算器不计为到账接入。
- 高级策略和风控输入的持久化及执行连接。当前只有九项页面配置可保存；账户记录筛选/导出和页面刷新测量已接线，深度筛选及其余七项遥测未完成。
- 单边未配对库存的风险政策与效果验证。当前候选放宽至 1.05 不代表动态补仓会越过最终 0.99 上限，也不能保证补齐。
- 资源和稳定性验收：预期负载下 CPU、盘口新鲜度、账户延迟、队列和事件完整性。

## CPU 与账户延迟

常驻增量盘口、账本空闲跳过与历史分析独立限额资源组已部署。约 20 秒样本中控制台 CPU 用时由 12.68 秒降为 3.76 秒；只读账户检查由 46.33 秒/504 改为 23.817 秒/200。分析仍在同机扫描 24 小时，限额 0.2 核；高 steal 和账户 SDK 冷启动仍存在。

这是短时测量，长期并发、纸面运行负载和资金闭环尚未完成。CloudWatch 积分和 EC2 credit mode 查询权限仍不足，未调整计费模式。完整证据见 [CPU 诊断](CPU-DIAGNOSIS-2026-09-10.md)。

当前修复批次回归为 Python248/1skip、引擎212、前端39，见 [账户数据记录](ACCOUNT-DATA-INTEGRATION-2026-09-10.md)；浏览器操作因工具网址识别失败未验收。
