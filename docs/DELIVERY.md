# 交付与验收状态

更新时间：2026-09-17。完整状态见 [当前系统状态](CURRENT-STATUS.md) 和 [交付规划 v8](STRATEGY-DELIVERY-PLAN-2026-09-13.md)。

## 当前结论

公共交易底座处于 **PARTIAL / REVIEW**：接口和 paper 能力已交付，真实异常生命周期、持续账户核对和控制台深度展示仍未完成。策略处于 **PAUSED**，自动实盘处于 **CLOSED**。服务器当前交易停止、paper 模式、实盘锁关闭。

## 已验证

| 范围 | 结果 | 边界 |
|---|---|---|
| 公共平台接口 | `TradingPlatform`、`TradingCore`、`StrategyPlugin` 已实现 | 真实异常组合仍需验收 |
| paper 执行 | BUY/SELL、多订单、FOK/FAK、部分成交、撤单和替换有回归 | 不代表真实队列和成交概率 |
| 真实适配器 | CLOB 签名、订单 ACK、撤单、用户事件和账户读取已接通 | 不等于连续自动交易 |
| 风险和存储 | 预留、日损失、持久化锁、恢复状态、重复成交和快照时间顺序有回归 | 账户全量联合核对仍缺 |
| 深度行情 | L2 买卖档位、真实 tick、时间和新鲜度字段已进入平台契约 | 控制台尚未展示完整五档 |
| 控制台运行 | 无策略平台启停、进程身份恢复、独立日志和状态/订单/成交投影已接通；订单集中在自动交易页，新增独立策略配置页 | 尚未自动换场，五档页面及完整延迟分布待补 |
| 工程验证 | 前端 72 项、类型检查和构建通过；引擎/Python 沿用最近完整基线 | 测试不是资金或收益保证 |

## 当前未完成

控制台已迁入公共平台；旧 `live.js run` 仅作显式兼容入口。本轮前端重构已发布，记录见 [前端重构发布记录](evidence/2026-09-17/frontend-refactor-release.md)。

1. 真实部分成交、撤改竞态、断线中成交、异常恢复和连续运行。
2. 余额、在途订单、持仓、费用、结算和充值提款的持续联合核对。
3. 结算到账、奖励付款和钱包净收益的完整对账。
4. 五档深度、订单生命周期分段延迟和自动调整原因的网页展示。
5. 服务器采集器恢复后的连续延迟、CPU、队列和告警样本。

## 策略与历史证据

历史 paper、参考地址回放、`PairCost`、`stableLive`、`target_clone` 和真实探针均保留在带日期的证据目录中，仅用于复盘。历史 `-$12.22` paper PnL 不能代表平台亏损或策略结论；单笔真实成交/撤单不能代表完整订单生命周期。

新的策略只能通过 `StrategyPlugin` 接入。底座审查完成前不恢复策略研究、不冻结默认参数、不启动自动实盘。

## 资金边界

未来受限验证仍使用本金 `$50`、北京时间单日损失 `$30`，并要求显式 live 解锁。文档整理和底座修复不会改变额度，也不会新增真实订单。
