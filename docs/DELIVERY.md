# 交付状态

更新：2026-09-17。版本 **`df9fd0d` 已部署**，主要代码 hash 已匹配。本次服务器检查：`running=false`、`live_unlocked=false`、`savedRevision=0`。当前已发布代码和页面，尚未保存启动反转策略，本轮没有真实订单证据。

## 已完成的代码与发布

| 范围 | 当前结果 |
| --- | --- |
| 策略 | 一个可配置 BTC 五分钟反转插件；触发、确认、阶段、预算、参数版本及恢复已接入 |
| 执行 | 公共底座处理限价单、撤单、用户回报、资金预留、费用来源及账户恢复 |
| 连续运行 | 当前/下一场发现与订阅、场次截止处理、旧场订单和结算保留 |
| 结算 | 标准 CTF 的 EOA/Deposit Wallet sender；原签名请求恢复、receipt 与 pUSD 到账核对 |
| 页面 | 独立策略参数/运行设置、阶段进度、UP/DOWN 盘口档位、订单 10/20/50 条分页、启停及暂停新增 |
| 发布 | 本地测试及类型检查已执行，代码部署和主要文件 hash 核对完成 |

结算模块 20 项定向测试及类型检查通过，包含成功、失败、等待回执、重复调用、崩溃恢复和 Relayer 临时拒绝。其他模块完整测试结果以当次发布记录为准；不以旧测试数量代替新版本结果。

## 当前剩余工作

1. 保存最小有效金额的测试参数、检查账户并开启本次实盘运行。
2. 采集真实下单 ACK、工作单撤销、买入成交、成交费用和账户变化；核对页面订单及延迟来源。
3. 在同一小额运行中核对重启恢复、跨场不串单、官方结算及有赢家持仓时的赎回到账。
4. 修复验证发现的问题，同步代码、服务器、文档和任务视图。

线上没有发生的部分成交、竞态或断线故障路径如实标为“快速测试覆盖，线上未出现”，不等待偶发情况数天。多日 paper、第三日期、午夜基线和原子快照不作为上线前置。

## 资金与历史材料

真实验证先用交易所最低有效金额。阶段份额、单场/总预算和可选日内停止线由用户配置，历史 `$50/$30` 不再作为固定限制，70/75 是同一逻辑的可编辑参考。

旧做市代码和带日期证据用于回退与复盘，不代表本次反转策略已真实成交，也不证明盈利。详细进度见 [当前系统状态](CURRENT-STATUS.md)、[开发规划](REVERSAL-DELIVERY-PLAN-2026-09-17.md) 和 [任务树](task-view.json)。
