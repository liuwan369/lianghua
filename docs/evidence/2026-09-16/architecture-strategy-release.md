# 功能架构与策略接口交付

日期：2026-09-16。代码提交：`8463da3`；运行状态文案修正：`1a653aa`。

- 任务视图增加功能架构，10 个模块 / 62 项功能：30 已完成、23 部分完成、7 待完成、2 延期增强。支持筛选、分组展开、负责人、验证情况和剩余工作；保留阶段任务。
- 执行 Agent 完成 BUY 策略接口、注册表、CLI 选择和观察策略；产品 Agent 完成页面、接口校验和测试；主 Agent 集成数据、规划、发布与浏览器检查。
- `pair-cost` 保持原有算法，`observe` 不产生订单且仅允许 paper。新 BUY 策略通过工厂注入；普通 SELL、多挂单、方向化成交 / 库存仍待扩展。详见 [策略接口](../../STRATEGY-INTERFACE.md)。
- 引擎 353 项 / 37 文件通过，类型检查与构建通过；前端 62 项 / 8 文件通过，构建通过。实际数据在桌面和手机筛选、详情、刷新均通过，无浏览器错误或横向溢出。

首批发布 `architecture-20260916T014221Z-59093f` 已完成 85 个差异文件的逐文件 SHA-256 核对。备份与清单在服务器 `/root/.pm-releases/architecture-20260916T014221Z-59093f/`，本地清单在 `.deploy/releases/architecture-20260916T014221Z-59093f/manifest.json`。

发布前后 `running=false`、`mode=paper`、`live_unlocked=false`，控制台与 collector 服务 active；没有下单、启动策略或修改资金配置。发布采用指定文件备份和逐文件替换，旧静态资源保留，不覆盖账户、行情、订单或风险状态。工作区其他研究和探针修改没有清除，也不宣称整个工作区三端一致。

线上入口：[控制台 → 任务视图](https://34-242-206-196.sslip.io/console/)。页面每 10 秒读取进度；完成状态由开发结果更新，不会因服务在线而自行变绿。
