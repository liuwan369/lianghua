# 控制台接入公共平台

日期：2026-09-16，北京时间。状态：代码验证完成，服务器发布验证待写入。

本批将控制台新启动、停止、进程身份恢复、日志投影和页面运行状态接入独立 `platform.js`。默认 paper、无策略，不产生订单；旧 CLI 仅保留显式兼容入口。每轮使用独立状态/日志，模拟账户不关联真实钱包。配置只应用 mode/duration，其余七项旧值保留并标为未应用。

验证：引擎 437 项 / 44 文件、前端 71 项、Python 567 passed / 1 skipped；类型检查和构建通过。独立代码复核发现的运行关闭、中文路径编码、过期状态和页面请求时效问题已修复。真实 Windows 隐藏进程的直接停止、持久化恢复后停止均退出 0，并完成日志清理刷盘。

本地真实公开行情试跑因本机采集器不存在、Gamma 访问超时，按 `starting -> platform_error -> failed` 正常退出，不能计作真实行情成功。线上行情验证以随后写入的结果为准。

剩余范围：自动换场、旧状态文件历史订单首次投影、完整延迟序列与页面五档展示。当前每次控制台新建状态文件；所选市场到期会正常停止。systemd 重启默认终止同组子进程，不代表交易可跨服务重启自动续跑。
