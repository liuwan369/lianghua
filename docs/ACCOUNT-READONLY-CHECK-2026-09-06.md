# 账户只读核对记录

最近核对：2026-09-07（北京时间）；下表中的 Relayer 信息来自 2026-09-06 历史记录。  
核对方式：登录后的 Polymarket 网页只读检查；未导出私钥、未创建密钥、未签名、未下单。

## 已核对

| 项目 | 结果 |
| --- | --- |
| 页面公开/API 地址 | `0xA693a0E0e40BDeC3d9d4a40bD4D087A5cECFD7cd` |
| 地址用途 | 页面明确提示“请勿向此地址发送资金。仅供 API 使用” |
| 现金 | `$15.71`（2026-09-07 网页只读核对） |
| 组合总额 | `$15.71`（2026-09-07 网页只读核对） |
| 当前持仓 | 无 |
| 登录方式 | 邮箱登录 |
| Relayer 签名地址 | `0xDa6F73818Af63191633D8c8025508CD703780CD0` |
| Relayer API 密钥 | 暂无；服务器也未配置 CLOB 签名凭据 |
| 真实订单 | 无 |

2026-09-07 本地文件 `C:\Users\Administrator\Desktop\量化密钥\Relayer API 密钥已创建.txt` 只读核对：`RELAYER_API_KEY` 字段仍是 `<已隐藏>` 占位符，不是可用密钥；`RELAYER_API_KEY_ADDRESS` 字段为真实地址，但不能代替 Deposit Wallet 地址或 Session Key。原文件未复制到仓库、未上传服务器、未在日志中输出。

## 未完成

- 资金归属、funder 地址和最终交易钱包需通过受支持的账户预检接口确认，不能用页面公开/API 地址代替。
- 都柏林服务器本次可连接，但当前仍未配置账户签名凭据；网页登录现金不能自动变成服务器 API 权限。
- 东京访问 Polymarket 曾返回 `JP blocked`，地区可用性未确认前不得启动真实订单。
- 真实 maker/taker 费用、返佣、奖励、订单回报和排队位置尚未产生账户证据。

## 放行顺序

1. 用户在服务器本地配置受支持的签名方式；私钥、密码、验证码不进入聊天和网页。
2. 只读预检确认账户类型、funder、签名方式、余额和授权范围。
3. 使用 post-only 极小额限价单，人工确认已挂出后立即撤单。
4. 核对订单回报、撤单回报、费用和账户余额变化；全部通过后仍保持低额观察。

当前裁决：`NOT_READY_FOR_LIVE_TRADING`。
