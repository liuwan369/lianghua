# 当前系统状态与接续点

更新：2026-09-17 23:15（北京时间）。主线为 [反转交付规划](REVERSAL-DELIVERY-PLAN-2026-09-17.md)。一个 BTC 五分钟反转策略，价格、阶段份额、阶段数和预算均可编辑；做市方向终止。

## 已交付的软件

- 反转插件：首样本区间入场、交替跨价、确认计数、保留旧方向订单、参数下场生效、暂停新增和状态恢复。28项策略测试通过。
- 执行底座：提交前保存签名身份、原签名恢复、ACK前成交、终态去重、FAILED冲正、真实费用覆盖估计、按市场隔离、连续换场及旧连接回收。
- 结算代码：官方结果、EOA/Deposit Wallet、pUSD适配器、持久签名与重试、回执和到账核对。尚无本轮真实赎回结果。
- 后台：策略配置版本化保存、真实控制接口、账户隔离状态、订单固定事件快照分页、真实成交额和费用投影。
- 前端：一个策略的中文表单、参数/运行二级标签、反转场次和阶段、5/10档深度、10/20/50订单分页、实际占用及未知数据提示。已检查桌面和390px窄屏。
- 资金流：pUSD/USDC.e确认日志、回执和官方活动分页归因已接入；pUSD转换只调整一次现金边界，游标和已应用流水随账本持久化。覆盖未追上现金观察时只显示暂估，不新增交易门槛。
- 回归：本批引擎49个文件/549项、前端完整71项、后台相关50项通过；独立复核无高、中、低问题。服务器主RPC上的既有交易只读样本正确归为trade，净外部资金为0。
- 本轮只读准备检查修复：行情投影误选0字节旧库，现按日期组和采集器标准文件选择，跳过无效库；午夜新库与同日归档覆盖回归已补。相关93项测试通过，独立复核通过，公网行情恢复。
- 账户API清理：移除通用账户快照中的旧50/30固定风险合同及原子快照前置文案；剩余策略预算不再按50减持仓成本伪造，真实限额以策略配置/运行时为准。相关36项测试和TypeScript构建通过；发布验收包含公网输出的只读核对。

## 实际服务器状态

行情修复版本 `bcbd1ba` 已推送并部署，633个发布文件SHA-256一致，控制台服务重启后公网行情恢复。账户API清理与本次文档纳入后续同批程序发布；服务器的最终版本以 `/root/.pm-releases/` 内manifest/result为准。

`running=false`、`mode=paper`、`execution=observation`、`strategy_id=null`、`live_unlocked=false` 是旧运行留下的停止状态，不表示新版在paper观察。策略配置已保存为 `savedRevision=2`：价格0.67/0.70/0.70、阶段份额[5,5,5,5]、单场预算14.50美元、总预算和日损null、持续运行。保存没有启动交易。服务器未配置交易控制密码，实盘开关关闭；本轮没有新增真实订单。

账户只读核对：余额106.313425美元，开放订单0，17个历史可赎回持仓当前价值合计0。官方当前最小单量5份；四阶段在0.70上限和当前费用峰值预留下合计14.35美元。原[5,18,54,130]是历史参考，未用它启动。最小份额用于功能验证，不证明原份额比例的收益。

行情修复回退包：`/root/.pm-releases/reversal-bcbd1ba-20260917T145228Z/before.tar.gz`；前版备份保留。完整改造前备份见[备份记录](evidence/2026-09-17/pre-reversal-backup.md)。程序发布不覆盖用户配置、凭据或账本；最低份额配置通过版本化API单独保存。回退程序不能覆盖后来产生的真实交易。

## 接续点

1. 检查当前Git和Agent状态，保留其他任务文件，不重做已完成实现。
2. 真实下单、撤单、赎回以及启动自动交易由用户操作；Agent只读核对真实回报并修复软件。没有真实证据的ACK/hash一致性、成交/费用、连续换场、赎回到账仍保留未验收。
3. 最低份额配置已准备；真实启动、下单、撤单、重启交易及赎回仍由用户执行。缺少控制密码与关闭的实盘开关如实列为运行配置未就绪，不能声称只点启动即可运行。

自动化 `btc` 当前已暂停；后续实际账户事件可再次触发只读核对。自动化不得代为金融交易。

## 分工

主Agent：Python配置/控制/投影、集成、文档和发布。策略Agent：`src/strategies/btc-reversal*`。执行Agent：`src/platform/`（结算除外）、`src/live/`、`src/cli/platform*`。结算Agent：结算代码及本轮指定文档。前端Agent：`web/src/`。独立复核只读查错，交原负责人修复。

历史研究、原始数据和其他任务未提交修改不混入本批提交。
