# 当前系统状态与接续点

更新：2026-09-17。主线为 [反转交付规划 v1.3](REVERSAL-DELIVERY-PLAN-2026-09-17.md)。一个 BTC 五分钟反转策略，价格、阶段份额、阶段数、预算均可编辑；停止做市方向。

## 实际进展

- R0：本地和服务器备份已完成，见[备份记录](evidence/2026-09-17/pre-reversal-backup.md)。
- 策略插件已落盘：首样本、交替跨价、确认计数、旧单保留、配置下场生效、持久化恢复、暂停新增。首轮22项测试及引擎构建通过。
- 前端已落盘：单策略普通表单、二级标签、反转场次/阶段/持仓/深度、10/20/50服务端分页、控制密码仅内存。首轮71项测试及构建通过。
- 后台已落盘：`/api/strategy-config` GET/PUT/POST、`/api/trading/control`、`/api/v1/orders`。按账户复用策略状态，配置版本冲突拒绝覆盖；翻页固定快照。相关首轮87项Python测试通过，随后终态账本与连续场次投影38项测试通过。
- 执行Agent正在完善：连续换场、旧连接回收、手续费预算、FAILED冲正及恢复。
- 结算Agent正在实现：真实钱包/relayer赎回发送、交易回执和到账确认。
- 独立复核已发现并推动修复：订单场次ID/slug不一致、预算漏费用、旧场连接增长、投影丢当前场、拒绝意图CREATED恢复不一致。修后仍需复核。

## 服务器事实

本次通过Python SSH实际读取：服务在线，`running=false`、`mode=paper`、`execution=observation`、`strategy_id=null`、`live_unlocked=false`。这仍是旧已部署版本；不代表本地新功能不存在，也不是要求再paper观察。本轮未新增真实订单，尚未部署新代码。

## 接续顺序（中转断开后从这里继续）

1. 检查Agent和Git状态，保留既有修改；不要重新写规划或重复实现。
2. 等执行/策略/结算修复落盘，核对CLI参数、运行投影、配置与前端一致。
3. 独立复核修复后差异，跑受影响集成、类型检查、构建，浏览器检查桌面与窄屏。
4. 仅提交本次文件，推送Git；备份此次替换文件并同步服务器源码/产物/清单。
5. 用交易所最低有效金额执行已授权真实验证，记录真实ACK、成交、撤单、恢复、官方结果和赎回到账。缺少真实结果的项目保留未完成。

Codex本任务续办自动化 `btc` 已创建，每30分钟检查并接续未完成项。没有变化时不重复通知。自动化不能保证中转故障期间执行成功，断点由本文件和任务视图保存。

## 本批文件所有权

主Agent：Python配置/控制/投影/分页、文档与发布；策略Agent：`src/strategies/btc-reversal*`；执行Agent：`src/platform/`（除结算文件）、`src/live/`、`src/cli/platform*`；结算Agent：`src/platform/live-settlement*`和`settlement.ts`；前端Agent：`web/src/`。独立复核不覆盖代码。

历史研究、原始数据、备份和其他任务未提交修改不混入本次提交。回退程序不能覆盖真实成交后的账本。
