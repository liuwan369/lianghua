# 当前系统状态与接续点

更新：2026-09-18 23:28（北京时间）。当前服务器程序发布基线为 `b9dbfcb21b911a2076b6608a7a0421f43ff25190`，发布目录 `/root/.pm-releases/reversal-b9dbfcb-20260918T152545Z`。主线为 [反转交付规划](REVERSAL-DELIVERY-PLAN-2026-09-17.md) v2.6，只维护一个可配置的 BTC 五分钟反转策略。

## 当前结论

反转策略、交易底座、账户与结算、普通用户控制台、独立 CLOB 行情投影和低延迟热路径均已发布。Market WebSocket 同一帧原子更新 UP/DOWN；最快买卖一与 L2 独立维护时序；策略没有固定采样、debounce 或 REST 盘口驱动。不同经济订单可并行，同一订单的提交、查回、撤单和重试严格串行。

三项 P1 已发布：Market/User WebSocket 坏帧立即标记异常并重连，User WS 重连后执行 REST 补偿；每场按 `endsAt` 精确触发场末；官方 `live/matched/delayed` 状态与内部生命周期分开保存，旧 HTTP ACK 不会把较新的 WS `matched` 倒退。场界前 10 秒预热下一场，停机先停止平台并撤工作单，再等待发现与恢复任务。

生产入口为 live-only。旧 maker 的 config、inventory、strategy、risk、engine、executor、orchestrator、研究 feed、注册表及对应测试已经从源码、包导出和服务器构建产物中物理删除；净减 7,817 行。旧 pm-r25 采集、SQLite 投影、分析、下载接口、服务、timer 及专属测试也已物理删除，服务器旧单元为 `not-found/inactive`。真实历史数据与回退版本保留。

## 当前真实状态

- 自动交易停止：`running=false`、`mode=null`、`strategy_id=null`、`live_unlocked=true`，开放订单 `0`。服务器解锁只为用户发起的最小真实验证准备，未代为启动交易；已停止的历史 paper run 只保留在历史接口，不再冒充当前状态。
- 账户最近只读现金为 `106.313425` 美元；当前有效持仓 `0`，17 项为 `redeemable=true/currentValue=0` 的历史零价值残留。
- 当前保存 revision 4 为最小真实验证配置：价格 `0.67 / 0.70 / 0.70`、阶段份额 `[5,5,5,5]`、单场预算 `14.5` 美元，总预算和日损为 `null`。原 revision 3 正式参数已备份到服务器私有发布目录；保存参数不会自动启动交易。
- 小额真实生命周期验证使用 `[5,5,5,5]`，只作为验证配置，不覆盖正式参数。真实金融操作由用户从页面发起；系统记录后由 Agent 只读核对并修复软件。

## 本轮运行准备与连接恢复

服务器配置已保存为 revision 4，原正式参数备份为 `/root/.pm-releases/validation-config-20260918T142257Z/formal-config.json`。当前 `durationMinutes=0` 表示启动后持续运行。当前未启动、无新增订单。

公共行情 WS 15 秒未收到任何消息或 PONG，或 30 秒未取得双边报价更新时，终止失效连接并进入既有重连；PONG 不刷新报价时间，正常事件无新增采样等待。页面区分已连接但盘口未齐、报价过期与断线。行情服务的 2000ms 报价年龄参数改用真实可读的 CLI 参数。

此发布的独立干净源码测试：引擎/策略/CLI 定向 `82/82`、前一基线引擎 `451/451`、前端 `82/82`；后台配置/快照定向 `43/43`。类型检查和生产构建通过；本补丁正在独立复核。前一界面验收1440/390页面无JS错误和横向溢出。停止后重新启动不会继承上一次运行的暂停状态；同一运行的明确暂停仍然保留。

## 已完成的软件交付

- Market WebSocket 原子双边更新；快 BBO 与 L2 独立时序；空盘口只标记盘口不完整，不把传输误判为断线。
- 反转状态机覆盖双边冲突、方向明确、同毫秒事件、重连基线和首样本入场，重连不会补造阶段。
- 阶段、资金预留和签名订单身份一次耐久提交；签名锁后重新检查余额、预算、开放订单和可卖份额。
- HTTP POST 延迟从实际 `fetch()` 开始计时；行情、盘口应用、策略判断、WAL、签名、HTTP ACK、用户回报和撤单分别计时。
- User WS 重连补偿、venue status、迟到成交、未知 ACK 查回、撤单竞态、重启恢复、换场、费用、官方结果和赎回到账投影均已实现。
- 总览区分当前有效持仓、待到账、待核对及历史残留，并显示 CPU、内存、磁盘、负载、关键进程和当前 run 的真实延迟。
- 场界 `1789721100` 的新场 `btc-updown-5m-1789721100` 在 `2026-09-18T08:45:00.250Z` 在线；外部采样约 0.49 秒看到 `online=true`、`connected=true`，随后 UP/DOWN 双边多档完整。
- 旧 maker 源码清理后引擎 `446/446` 通过，类型检查和生产构建通过，独立复核 APPROVE；发布时清理了 150 个服务器旧源码或生成产物。
- 旧 pm-r25 生产残留清理和停机状态投影修复后，定向回归 `93/93`、后台保留范围 `541` 项通过和 `1` 项跳过，独立复核通过。
- 启动恢复状态修复已发布：新运行默认恢复新增交易，真实停止仍撤余量并保留已成交持仓；每 5 分钟只读自动巡检已启用，异常时由现有 Agent 复核软件并通知，不代为交易。

前一热路径发布验证为引擎 `575/575`、P1 定向 `152/152`、TypeScript 类型检查及生产构建通过，独立复核无 CRITICAL/HIGH/MEDIUM。清理后 Python 保留范围为 `541` 项通过、`1` 项跳过（排除三个既有研究测试文件）；前端当前回归为 `82` 项通过。

## 尚未完成

1. 用户从页面启动 `[5,5,5,5]` 最小真实运行后，核对真实 ACK、成交、撤单、User WS、重启恢复、实际费用、场次收益和结算到账。
2. 用户确认正式参数及每场预算后，核对配置版本、自动换场、旧场收尾和连续结算。没有发生的真实证据保持未完成，不宣称盈利。
3. 整数单位统一、engine/运行数据目录迁移及仍带未提交修改的 Python/研究文件整理属于后续非阻塞工作，不增加真实生命周期验证的等待条件。

## 分工与回退

执行热路径 Agent 维护 Market/User WebSocket、CLOB、WAL 和订单并发；策略与账户 Agent 维护反转状态机和账户分类；后台与前端 Agent 维护延迟、系统指标和页面；主 Agent 负责集成、瘦身、任务树、发布和只读验收；复核 Agent 独立查错。

本地程序源码、Git 提交和服务器发布已同步到 `924f4e6`；后续文档提交只更新状态说明、部署脚本与任务视图。本次程序发布归档 SHA-256 为 `40adb2dc3c95f28180243d87a41714eb3b4395f56d349b8d61a60fab3a3289eb`，校验 `497` 个文件，更新 `20` 个、删除 `1` 个。程序回退只替换程序和静态资源，不能用旧备份覆盖新成交后的订单、持仓、资金或结算状态。
