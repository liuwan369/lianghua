# 当前系统状态与接续点

更新：2026-09-17。主线为 [反转交付规划](REVERSAL-DELIVERY-PLAN-2026-09-17.md)。一个 BTC 五分钟反转策略，价格、阶段份额、阶段数和预算均可编辑；做市方向终止。

## 已交付的软件

- 反转插件：首样本区间入场、交替跨价、确认计数、保留旧方向订单、参数下场生效、暂停新增和状态恢复。28项策略测试通过。
- 执行底座：提交前保存签名身份、原签名恢复、ACK前成交、终态去重、FAILED冲正、真实费用覆盖估计、按市场隔离、连续换场及旧连接回收。
- 结算代码：官方结果、EOA/Deposit Wallet、pUSD适配器、持久签名与重试、回执和到账核对。尚无本轮真实赎回结果。
- 后台：策略配置版本化保存、真实控制接口、账户隔离状态、订单固定事件快照分页、真实成交额和费用投影。
- 前端：一个策略的中文表单、参数/运行二级标签、反转场次和阶段、5/10档深度、10/20/50订单分页、实际占用及未知数据提示。已检查桌面和390px窄屏。
- 回归：执行Agent最终47个引擎测试文件/497项通过；前端完整71项通过；后台配置/控制/投影相关39项通过。后续小修以发布记录为准。

## 实际服务器状态

代码提交 `5166c3d` 已推送 Git，并部署引擎、后台及控制台。通过 SSH 实际核对关键源码、dist与前端资源 SHA-256 一致；配置读取、指定run的订单分页、网页返回正常；控制台和采集服务 active。

`running=false`、`mode=paper`、`execution=observation`、`strategy_id=null`、`live_unlocked=false` 是旧运行留下的停止状态，不表示新版在paper观察。新版策略配置尚未保存（savedRevision=0）；服务器未配置交易控制密码。本轮没有新增真实订单，也没有自动启动策略。

程序替换前备份：`/root/.pm-releases/reversal-5166c3d/before.tar.gz`。完整改造前备份见[备份记录](evidence/2026-09-17/pre-reversal-backup.md)。用户配置、账户凭据、账本、运行状态均未覆盖；本次程序回退不能覆盖后来产生的真实交易。

## 接续点

1. 检查当前Git和Agent状态，保留其他任务文件，不重做已完成实现。
2. 收尾SDK返回对象形式404的原签名恢复、页面未保存提示及最终复核，更新同批构建并核对服务器。
3. 真实资金流对日内损失基线的自动修正尚未完整接入：核心支持显式外部资金调整，当前live恢复仍传0。不能声称充值/提现已从损益中完整剔除；不把等待午夜作为解决办法。
4. 真实下单、撤单、赎回以及启动自动交易由用户操作；Agent继续开发、只读核对真实回报、诊断和修复。没有真实证据的ACK/hash一致性、成交/费用、连续换场、赎回到账仍保留未验收。
5. 所有可执行的软件交付事项完成后暂停开发续办自动化；后续实际账户事件可再次触发只读核对，不循环重复通知。

自动化 `btc` 已配置为每30分钟接续开发检查，主线未完成时避免重复Agent。无变化时安静；中转失效不保证调度成功，以上文件作为断点。自动化不得代为金融交易。

## 分工

主Agent：Python配置/控制/投影、集成、文档和发布。策略Agent：`src/strategies/btc-reversal*`。执行Agent：`src/platform/`（结算除外）、`src/live/`、`src/cli/platform*`。结算Agent：结算代码及本轮指定文档。前端Agent：`web/src/`。独立复核只读查错，交原负责人修复。

历史研究、原始数据和其他任务未提交修改不混入本批提交。
