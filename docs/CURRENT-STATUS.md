# 当前系统状态与接续点

更新：2026-09-18 05:16（北京时间）。当前服务器发布基线为 `70d2b55`；本地候选版已完成生产热路径、真实持仓口径、延迟和服务器指标修复，正在执行三段同步。主线为 [反转交付规划](REVERSAL-DELIVERY-PLAN-2026-09-17.md) v1.9，只维护一个可配置的 BTC 五分钟反转策略。

## 当前结论

四项 P0 已在本地修复并回归：同一 WebSocket 双边批次原子应用、`best_bid_ask` 快事件连续处理、双边冲突后的方向解析、已结算零价值残留不进入活动仓位。交易路径由 Market WebSocket 逐事件驱动，没有固定 1 秒采样、debounce 或 REST 盘口轮询；不同订单可并行，同一经济订单严格串行。

总览已区分当前有效持仓、待到账、待核对和已结算零价值历史残留；当前有效持仓应为 `0`。账户历史明确标记为整个钱包范围，不冒充当前策略订单。页面已接 CPU、内存、磁盘、负载和关键进程状态，并按当前 `run_id` 展示分段延迟的 latest、p50、p95、p99、max 和样本数。

## 当前真实状态

- 服务器自动交易停止：`running=false`、`strategy_id=null`、`live_unlocked=false`，开放订单 `0`。
- 账户最近只读值为现金 `106.313425` 美元；截图中的 17 项均为 `redeemable=true/currentValue=0` 的已结算零价值残留，不是当前有效持仓。
- 已保存 revision 3：价格 `0.67 / 0.70 / 0.70`、阶段份额 `[5,18,54,130]`、单场预算 `14.5` 美元，总预算和日损为 `null`。保存参数不会自动启动交易。
- 小额真实生命周期验证使用 `[5,5,5,5]`，只作为验证配置，不覆盖正式参数。真实金融操作由用户发起；系统记录后由 Agent 只读核对。

## 本地候选版完成

- Market WebSocket 同一帧先完整更新 UP/DOWN，再只调用一次策略；快买卖一和 L2 采用独立时序，同毫秒后到的 L2 可以恢复为权威深度，盘口变空会进入不健康状态。
- 反转状态机加入 `pendingAmbiguity`，覆盖双边冲突后方向明确、同毫秒价格变化及重连仅重建基线。
- 阶段和资金在单场状态机内分配；不同订单 HTTP 在途可并行，同一订单提交/查回/撤单/重试去重串行。等待短签名锁后重新检查余额、单场预算、开放订单和可卖份额。
- 发单前把阶段、预留和签名身份合为一次关键耐久提交；HTTP ACK 不占用全局锁。异常 ACK、明确拒单和未知订单的最终状态均强制写盘。
- 延迟贯通行情年龄、盘口应用、策略判断、耐久提交、签名、HTTP 发出、ACK、用户成交回报、撤单和整轮反应；没有样本时继续显示未知，不用模拟数字补齐。
- 后台增加缓存式系统资源指标；无效或复用 PID 不显示为健康交易进程。

验证结果：引擎完整 `49` 文件、`583` 项通过；前端 `81` 项通过并完成生产构建；后台 `605` 项通过、`1` 项跳过；引擎类型检查和两套生产构建通过。最终独立复核收尾后提交、推送、备份和部署。

## 尚未完成

1. 将本地候选版提交、推送并发布到服务器，核对公网 API、总览、手机/桌面布局和发布哈希。
2. 按生产依赖图执行 live-only 瘦身：解除生产引用后删除旧做市、paper、回测、研究入口、旧网页和过时文档；保留真实交易证据、秘密、运行状态及回退包。
3. 用户启动 `[5,5,5,5]` 小额真实运行后，核对真实 ACK、成交、撤单、重启恢复、实际费用、场次收益和结算到账。
4. 用户确认正式参数后，核对自动换场、旧场收尾和连续结算。以上真实证据尚未发生，不能标为完成或宣称盈利。

## 分工与回退

执行热路径 Agent 负责 Market/User WebSocket、CLOB、WAL 和订单并发；策略与账户 Agent 负责反转状态机和持仓分类；后台与前端 Agent 负责延迟、系统指标和页面；主 Agent 负责集成、瘦身、任务树、发布和只读验收；复核 Agent 独立查错。

发布保持本地源码、Git 提交、服务器清单三段一致。程序回退只替换程序和静态资源，不用旧备份覆盖新成交后的订单、持仓、资金或结算状态。
