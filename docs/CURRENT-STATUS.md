# 当前系统状态与接续点

更新：2026-09-18 13:00（北京时间）。当前服务器发布基线为 `46d1cb5b3603afcf03e89390b588385ebd3580be`，发布目录 `/root/.pm-releases/reversal-46d1cb5-20260918T045546Z`。live-only 瘦身版和独立只读 CLOB 行情投影已构建、推送并发布。主线为 [反转交付规划](REVERSAL-DELIVERY-PLAN-2026-09-17.md) v2.3，只维护一个可配置的 BTC 五分钟反转策略。

## 当前结论

生产热路径、真实持仓口径、延迟和服务器资源指标修复已经提交、推送并发布。Market WebSocket 同一帧原子更新 UP/DOWN；最快买卖一和 L2 使用独立时序；策略没有固定采样、debounce 或 REST 盘口驱动。不同经济订单可以并行，同一订单的提交、查回、撤单和重试严格串行。

已发布版删除生产 `PaperGateway`、paper/backtest/旧 maker CLI、旧 PM2/脚本入口和旧静态网页；控制台和引擎新启动只能显式使用 live，历史 paper 日志与配置仍可只读。停机总览已改读独立 CLOB Market WebSocket 原子快照；旧 pm-r25 采集器暂保留作回退和历史证据源，控制台不再依赖它。

总览已区分当前有效持仓、待到账、待核对和已结算零价值历史残留。当前有效持仓为 `0`，账户页的 17 项是 `redeemable=true/currentValue=0` 的历史残留，不是当前仓位。账户历史明确标记为整个钱包范围，不冒充当前策略订单。页面已显示 CPU、内存、磁盘、负载和关键进程状态；延迟只展示当前 `run_id` 的真实样本，15 秒过期，无样本显示 `--`。

## 当前真实状态

- 自动交易停止：`running=false`、`strategy_id=null`、`live_unlocked=false`，开放订单 `0`。
- 账户最近只读现金为 `106.313425` 美元；当前有效持仓 `0`，历史零价值残留 `17` 项。
- 已保存 revision 3：价格 `0.67 / 0.70 / 0.70`、阶段份额 `[5,18,54,130]`、单场预算 `14.5` 美元，总预算和日损为 `null`。保存参数不会自动启动交易。
- 小额真实生命周期验证使用 `[5,5,5,5]`，只作为验证配置，不覆盖正式参数。真实金融操作由用户在页面发起；系统记录后由 Agent 只读核对并修复软件。

## 已完成的软件交付

- Market WebSocket 同一帧先完整更新 UP/DOWN，再调用一次策略；较新 BBO 存在时不把旧 L2 数量和深度标成实时数据，L2 追平后恢复权威深度。
- 反转状态机的 `pendingAmbiguity` 覆盖双边冲突、随后方向明确、同毫秒事件和重连；重连只重建基线，不补造阶段。
- 阶段、资金预留和签名订单身份一次耐久提交；等待签名锁后重新检查余额、预算、开放订单和可卖份额。
- HTTP POST 延迟从真正调用 `fetch()` 时起算；异步请求头和本地处理归入本地触发至 POST 分段，不再混入网络 ACK。
- 总览显示真实持仓分类、钱包历史口径、CPU、内存、磁盘、负载、dashboard、collector、trader 和 projection 进程。
- 桌面 1440px 和手机 390px 页面均无横向溢出、无脚本错误；长地址和单列网格不会撑宽页面。

本批验证结果：引擎 `544` 项通过；前端 `81` 项通过；后台受影响定向回归通过（含策略配置、控制、路由和旧进程恢复）；引擎类型检查、隔离生产构建和前端生产构建通过。发布清单校验 `613` 个文件，更新 `55` 个、删除 `145` 个；独立复核无 CRITICAL/HIGH 问题。

## 尚未完成

1. 观察独立 CLOB WebSocket 投影跨场稳定后，停用旧 pm-r25 停机行情依赖并清理剩余旧做市、研究入口和旧网页。保留真实交易证据、秘密、运行状态及至少一个回退版本。
2. 用户启动 `[5,5,5,5]` 小额真实运行后，核对真实 ACK、成交、撤单、重启恢复、实际费用、场次收益和结算到账。
3. 用户确认正式参数后，核对每场配置版本、自动换场、旧场收尾和连续结算。未发生的真实证据保持未完成，不宣称盈利。

## 分工与回退

执行热路径 Agent 负责 Market/User WebSocket、CLOB、WAL 和订单并发；策略与账户 Agent 负责反转状态机和持仓分类；后台与前端 Agent 负责延迟、系统指标和页面；主 Agent 负责集成、live-only 瘦身、任务树、发布和只读验收；复核 Agent 独立查错。

本地源码、Git 提交和服务器发布保持三段一致。当前发布归档 SHA-256 为 `b6adb9a309dbd6ed2a167f76894810c45286ba61c7d9a9d5dd0d56ef7161cc23`，共校验 `635` 个文件，本批更新 `25` 个、删除 `0` 个。新行情服务已 active/enabled，公网快照显示 `source=polymarket-ws`、双边五档和约 13ms 行情年龄；交易仍为 stopped/locked。程序回退只替换程序和静态资源，不能用旧备份覆盖新成交后的订单、持仓、资金或结算状态。
