# 当前接手上下文

更新日期：2026-09-10。

## 项目与部署

项目是 Polymarket BTC 五分钟 Up/Down 对冲量化系统。唯一当前节点为 AWS 都柏林 `eu-west-1`，公网 IP `34.242.206.196`，应用目录 `/root/pm-system`。

正式入口：<https://34-242-206-196.sslip.io/console/>。控制台服务为 `pm-system-dashboard-dublin.service`，采集服务为 `pm-r25-dublin-collector.service`。本机 `127.0.0.1:8765` 只用于本地开发，不是用户正式入口。

公网登录认证已按用户明确要求取消。账户检查、配置保存与实盘解锁是不同能力；保存账户不自动下单，也不广播链上授权。禁止在文档、日志或工具输出中复制钱包私钥/API secret。

## 必须保持的设计

用户确认的六页是总览、自动交易、市场、订单、收益、设置。设计基线为提交 `269f8ab`、标签 `ui-demo-2026-09-09`。保持完整布局、样式和模块，不擅自简化、替换或删减。正式工程位于 `web/`，原设计资源位于 `docs/demo-*`。

所有展示必须区分真实数据、未接入、无样本、过期及纸面结果。未知金额不能显示成零；HTTP 200、源码测试通过和纸面运行都不能作为真实交易或盈利证明。

## 当前功能边界

公网状态、配置、市场、运行记录和账户状态读取已接入都柏林，六页保留原设计。设置的 `order/life/mode/duration/submitted/maxOrders` 六项映射服务器配置，其余原设计策略字段仍是草稿。运行事件可以查看；真实未完成委托与逐笔成交另有来源切换、筛选、分页和 CSV 导出，完整逐委托生命周期及已撤历史仍缺来源。

网页已接线版本化纸面模拟启停，实盘仍锁定。首页已接线 CLOB 抵押余额、持仓和关闭持仓报告盈亏；抵押余额不冒充扣除占用后的可花余额。真实手续费、奖励到账和完整钱包对账仍缺来源；页面刷新已有5分钟测量，其余七环节及整轮延迟未完整接入。

## 当前核验证据

2026-09-10 的实际 CLOB V2 路线所需授权缺项为 0，签名匹配。链上与私有 CLOB 可交易余额查询均得到 `108.728676`；这是测量值，不是实时余额。私有只读查询首屏当前订单 0 条、成交 19 条，未同步全历史。

最新账户数据接入回归：Python248项通过、1项跳过，引擎208项、前端32项及构建通过。35个发布文件哈希一致，账户配置未改变，实盘仍锁定；证据见 [账户发布记录](evidence/2026-09-10/account-data-release-check.json)。

最终版本 90 秒真实行情 paper 记录 `DOWN 20 @ 0.18`、成本 $3.60，停止时未配对且未结算；它不是已结算亏损，也未证明对冲成功。另一次六分钟 paper 出现推断结算 -$4.60。真实 ACK、成交、撤单、结算资金对账仍未真钱验收。

## 策略和运行风险

maker 决策、费用预算与执行按每个 token 的真实 tick 向下量化，缺少 tick 时拒绝。最低委托不能自动扩大策略批准数量。挂单占用、部分成交剩余量、待终态吃单、迟到回报和停止最终对账已有回归约束。

`target_clone` 的动态补腿最终按 `hedgePairCostCeiling=0.99` 检查；紧急候选阈值不保证最终可成交或必然平仓。补腿成本超限、预算不足或残余数量不足最低份数时仍可能单边持仓。未验收长期收益、真实队列概率和完整退出策略。

常驻增量盘口、账本空闲跳过及同机历史分析资源隔离已部署。约 20 秒样本中控制台 CPU 用时从 12.68 降至 3.76 秒；账户检查从 46.33 秒/504 变为 23.817 秒/200。历史分析限额为 0.2 核，仍扫描 24 小时。SDK 冷启动、长时间负载稳定性及高 steal 仍待处理；CPU 积分与 credit 模式因 IAM 只读权限不足尚未核实。

完整证据与限制见 [实盘准备度验收](LIVE-READINESS-AUDIT-2026-09-10.md)、[CPU 诊断](CPU-DIAGNOSIS-2026-09-10.md)。待办按 [前端接入清单](FRONTEND-INTEGRATION-PLAN.md) 和 [当前进展](PROGRESS.md) 执行。

真实只读查询取得19成交、8持仓、3关闭持仓、22活动。Windows浏览器工具无法识别URL而停止，本轮浏览器验收未通过。详见 [接入记录](ACCOUNT-DATA-INTEGRATION-2026-09-10.md)。
