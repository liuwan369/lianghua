# 账户接入与操作验收（2026-09-08）

结论：账户配置和纸面启停已部署并通过下列检查；整个真钱交易系统尚未验收，未证实策略能稳定盈利。

## 测试证据

| 项目 | 结果 |
| --- | --- |
| 本地 Python 回归 | 99 passed，1 skipped（Linux 权限测试） |
| 都柏林账户回归 | 18 passed，包含目录 700 / 文件 600 |
| TypeScript 构建与回归 | 构建通过，101 passed |
| 账户接入最终代码复审 | 无新增可操作问题；不代表实盘通过 |
| Nginx / 服务 | 配置测试通过；服务 active；未登录公网账户接口 401 |
| 真实行情纸面自动停止 | 60 秒；2 挂单、1 成交、1 撤单，成交额 $4.998；退出码 0 |
| 手动启动 / 停止 | 启动返回 running=true，停止返回 false，无真实挂单 |
| 已存账户只读核对 | 2026-09-08 10:57:11 UTC：链上 $14.655381，Owner 匹配，缺 1 项资金及 2 项持仓授权；compromised=true，account_ready=false |
| 公网浏览器操作 | 等待用户完成访问登录；不以接口测试冒充页面已验收 |

纸面运行原始记录：服务器 `_external/btc-5m-market-trading-bot/results/paper/dashboard-20260908-104928.jsonl`。成交为 Up 10.2 份 × $0.49；另一边未成交并撤单。停止时该市场尚未结算，收益为 null，不能按零盈亏或盈利记账。本轮另一个手动启停测试会成为页面“本次运行”，上述记录仍保留。

只读检查通过 SSH 在服务器回环接口调用，模拟受认证 HTTPS 代理头以检验后端，未读取或输出原始凭据。此项不替代用户浏览器登录后的端到端测试。备用隧道页面已打开并显示四个入口，完整交互验收尚未完成。

部署保留回滚文件 `/root/pm-onboarding-backup.udQ23o`；未复制账户秘密到部署压缩包。部署未广播授权、未下真钱单、未转账。

## 当前产品边界

- 单管理员使用。账户页通过受认证 HTTPS 提交配置；服务器保存，密码不回显。
- 策略配置保存在当前浏览器，不支持跨浏览器同步。
- 账户检查为只读，只核对已实现的钱包类型、签名关系、链上余额及授权；不是实际接单成功证明。
- 真实排队序号无法从公开盘口直接得出，纸面队列是估算。
- 内置一键链上授权、真实接单/回报、部分成交、断线恢复、费用/返佣/奖励与结算对账仍未完成整体实盘验收。
- 现有旧 Owner/Relayer 凭据有暴露记录。检查拒绝旧 Owner；实盘锁保持关闭。

## 需要用户提供或完成

1. 在公网页面完成访问登录，以验收用户操作流程；不用把访问密码发聊天。
2. 更换安全签名账户及 Relayer Key，并通过账户页自行填写。Owner 私钥不能仅改登录密码来更换；当前资金钱包如何换 Owner 或迁移应按官方支持流程确认，不能假设页面改地址即完成。

拿到安全账户后，先只读预检，再核对具体链上授权范围，最后按用户已要求的极小额挂单/撤单范围开展真实验收。不得用暴露凭据测试，也不得直接启动无限期真钱策略。
