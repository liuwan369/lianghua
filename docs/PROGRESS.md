# 项目进度

更新时间：2026-09-09

## 新前端设计冻结与接入规划（2026-09-09）

- 用户认可六页演示设计，保存回滚标签 `ui-demo-2026-09-09`，对应提交 `269f8ab`；不是实盘功能验收。
- 已依据本地源码核对现有 API、参数与模块，形成 [接入及逐项验收方案](FRONTEND-INTEGRATION-PLAN.md)。正式前端未替换，服务器未变更。
- 后续依次完成契约/配置/账本拆分、六页只读接入、参数及启停闭环、奖励/延迟、故障及部署验收、极小额实盘独立验收。每批记录实际证据；缺数据或未实现不得标通过。
- 当前关键缺口：演示参数没有完整引擎映射、跨运行统一账本与奖励对账未完成、八个延迟点未全部接入、新页面端到端未验收。下述服务器状态和测试数字仍为此前记录。

## 模块化接入第一批（2026-09-09）

- 已加入版本化配置存储、独立订单账本和低优先级投影进程；页面请求不再负责全量日志统计，避免影响交易控制与行情热路径。
- 已加入 `/api/v1/` 配置/状态/市场/运行/事件/汇总接口及版本绑定的模拟启动入口；正式六页尚未接入这些接口，旧入口未替换。
- 已验证重复启动、配置版本冲突、日志截断/重启、重复成交回报、空结算、统计变慢、旧运行尾部追平和过期快照。Python `99 passed, 1 skipped`；Node build/Vitest `101/101`。
- 本地合成性能证据：75,000 次引擎决策，后台投影开启时决策 P95 约 `0.0024–0.0033ms`；这不是交易所网络延迟证明，需都柏林同机真实行情长测后才能定论。
- 本批未部署服务器、未打开真钱、未声明收益或实盘完成。下一批是正式前端只读接入和 TypeScript 日志/FeedQueue 热路径压测。

## 本轮账户接入（2026-09-08）

- 用户可在“账户”页填写并只读检查、保存账户；服务器原子持久化并校验权限，秘密不回显、不进浏览器存储。
- 修复换账户继承旧密钥、保存与启动竞态、所检查钱包与执行钱包不一致、空保存与损坏配置回退问题。
- 前端补齐实盘启动确认，避免重复点击，错误提示保留；行情获取失败清空旧价格，未知价格显示 `--`。
- 已部署都柏林。Python 本地 99 通过、1 跳过，Linux 账户套件 18 通过；TypeScript 构建及 101 项测试通过。最终代码复审未发现新增问题。
- 1 分钟服务器纸面验证：2 挂单、1 成交、1 撤单，成交额 $4.998，正常退出；尚未结算。另验证手动启动/停止。没有真钱订单。
- 已知暴露 Owner 被检查拒绝。真实订单验收未完成；缺少系统内一键链上授权、真实账户费用/奖励对账。详见 [本轮验收](ACCEPTANCE-2026-09-08.md)。

## 前序已完成（历史记录）

- 对目标地址 `0x3048d65321be3497164cdfc2996f94f98a2e7537` 完成历史行为分析：主要在 BTC 5 分钟市场高频双边买入并动态补轻的一边。
- 完成动态配对成本、补仓数量、资金上限、单边库存、临近结束和价格反向风控。
- 完成历史回放、参数扫描、最坏结算和费用情景分析。
- 完成普通用户页面：交易、账户、配置、订单。
- 页面能够显示真实盘口以及纸面挂单、成交、撤单、成交额、手续费字段、持仓和结算盈亏。
- TypeScript 引擎具备纸面路径和受保护实盘路径；实盘加入主行情/用户回报双 WS 门槛、`250ms` 行情新鲜度、连接复用、本地签名、ACK 计时和状态不明时失败关闭。
- 完成部分成交记账、成交早于 ACK 的缓存、撤单核对、账户成交重建和断线停止逻辑。
- 都柏林主节点部署完成：采集器、页面服务、小时分析和每日轮换服务均为 `active/enabled`。
- 已部署带有效证书、登录保护和限速的公网 HTTPS 页面；未登录返回 401，登录后页面和实时接口返回 200。SSH 隧道保留为备用。
- 都柏林 30 分钟网络基线完成：REST `p50=26.19ms / p95=35.38ms`，WS 建连 `p50=46.64ms`，稳定盘口消息年龄 `p50=9ms / p95=86ms`，约 51 万条消息，0 错误、0 陈旧事件。
- 内核短时 A/B 已完成：16MB TCP 缓冲区候选的消息年龄 P95 聚合值约 `52.50ms`，默认约 `63ms`，改善约 `16.7%`；REST P95 和收包间隔略差，且没有长测确认，因此服务器保持默认内核参数。
- Node WebSocket 已验证实际开启 `TCP_NODELAY`。
- 修复都柏林纸面引擎仍连接旧 `8765` 端口的问题，统一由 `PM_LIVE_URL` 指向本机 `18766/api/live`。
- 修复单笔美元上限只限制执行器、策略仍按原始份数记账的问题；策略仓位、日志和实际提交现在统一使用执行器最终价格与份数。份数改为向下取整，并硬性保证名义金额不超过用户上限。
- 都柏林真实行情 `$1` 纸面验证：成功产生两次 Up 限价挂单，每次 `14.28` 份、价格 `$0.07`、名义金额 `$0.9996`，均在 15 秒后撤单，0 成交、0 扣款；采集接口无连接错误，累计提交额达到本次 `$2` 限制后拒绝继续挂单。
- 最近完整回归记录：Python `82 passed`；TypeScript 类型检查和构建通过，Vitest `101/101 passed`。
- 建立 GitHub 云仓库 `liuwan369/lianghua`；源码、配置模板、测试和当前文档可回滚，密钥与原始数据库不上传。
- 接入官方 `@polymarket/client 0.9.0` 的公开只读账户检查，确认资金地址是 Deposit Wallet，Owner 为 `0xDa6F...CD0`，不再误报 EOA。
- 官方 SDK 实测该钱包尚缺 1 项 ERC-20 和 2 项 ERC-1155 交易授权；预检现已失败关闭。
- 账户配置严格区分资金钱包、Owner、Session Key、Relayer 和 Builder；占位符、错误新字段、旧私钥回退均不能放行实盘。
- 都柏林 Node 已从 22 升级到 `24.13.0`；官方安装包 SHA-256 校验通过，旧版本保留可回退。
- maker 订单改为 GTC 后由程序主动撤单，去掉不合规的 120 秒 GTD；最小订单量改为逐市场官方参数，缺失时拒绝真实下单。
- 当前加密市场费率参数由 `0.072` 修正为平台参数 `0.07`。
- 将旧下单依赖中的 `ws` 固定到安全的 `8.21.x` 版本；当前官方 npm 审计为高危 `0`、中危 `0`，剩余 `12` 个低危项来自官方 CLOB SDK 的 ethers/elliptic 传递依赖。
- 官方 Builder 团队已明确：自建 bot/MM bot 使用 Relayer API Key，不使用 Builder Key；Session Key 目前仅向选定合作方 Beta 开放。项目已删除“必须申请 Builder 才能交易”的错误前提。
- 已只读验证本地 Relayer API Key：官方认证接口返回 HTTP 200 和有效 nonce；Deposit Wallet 返回 `WALLET deployed=true`。验证未签名、未授权、未广播，也未将 Key 写入都柏林磁盘。
- 微观结构门槛现在会在每次新盘口到达时重新检查已有挂单；当可见排队量、主动卖单速度或盘口数据不再支持安全成交概率时，自动发出撤单，避免旧挂单继续暴露。
- 行情 WS 的 `tick_size_change` 会同步到盘口快照，策略估算价格与执行器价格舍入使用同一市场 tick；新增动态撤单回归测试。
- 本轮回归：Vitest `101/101`、Python `82/82`、TypeScript build/typecheck 均通过；密钥扫描未发现私钥或 Session Key 内容。

## 当前运行状态

- 主节点：都柏林。
- 公开数据采集：运行中。
- 页面服务：运行中。
- 小时分析与每日轮换：运行中。
- 纸面引擎：按用户点击启停，当前服务器检查时未运行。
- 真钱授权：关闭。
- 账户资金地址：已配置并完成公开只读核对。
- 账户交易签名：旧 Owner 与链上匹配，但凭据有暴露记录，不能用于实盘验收；需更换安全签名账户及 Relayer Key。Session Key 不是必需项。
- 网页账户只读核对：地址 `0xA693...D7cd`，可用现金 `$15.71`；该网页登录账户尚未授权给都柏林交易程序。
- 当前裁决：`NOT_READY_FOR_LIVE_TRADING`。

## 尚未完成

1. 在都柏林用更长的相同负载复核 16MB TCP 缓冲区；未通过前不修改默认内核。
2. 旧 ethers 依赖仍有低危密码学告警；待下单路径完全迁移到新版官方 SDK 后删除旧 CLOB SDK。
3. 不等待 Session Key；先更换安全账户及 Relayer Key，重新确认 Owner/资金归属，再补齐官方授权。旧暴露凭据不得继续广播。
4. Owner 签名方案完成（或收到官方 Session Key Beta 邀请）后，做极小额 post-only 挂单/撤单，测真实 ACK、排队、部分成交和 maker/taker 身份。
5. 演练 WebSocket 断线、订单状态不明、撤单失败、仓位重建和单边库存止损。
6. 逐笔闭环真实手续费、做市返佣、流动性奖励和最终结算。
7. 纸面与小额实盘门槛都通过后，才讨论连续自动运行和扩大资金。

账户配置软件路径已完成本轮检查，但一键链上授权流程、旧 SDK 迁移等仍有独立开发工作；不能把全部剩余事项都归因于等待用户。真实订单验收需要有效的安全签名账户及官方授权，不会用模拟数据冒充完成。

## 结论

当前已经是“真实数据 + 可操作纸面交易 + 受保护实盘代码”的系统，不是已经验收完成的真钱交易产品，也没有证据证明稳定盈利。
