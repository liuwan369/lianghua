# 从这里开始

控制台：**https://34-242-206-196.sslip.io/console/**。当前在线仍是已有交易平台，尚未部署反转策略界面。

2026-09-17 起停止做市方向，当前以 [BTC五分钟反转开发规划](docs/REVERSAL-DELIVERY-PLAN-2026-09-17.md)、[技术评估](docs/REVERSAL-TECHNICAL-ASSESSMENT-2026-09-17.md)、[Agent协作规则](docs/AGENT-WORKFLOW.md) 和 [当前状态](docs/CURRENT-STATUS.md) 为准。

## 当前页面

现有入口为总览、自动交易、策略、收益、设置、任务视图。订单位于自动交易页，策略设置仍是旧会话草稿，不能当作反转配置已保存或已运行。新页面计划为70/75方案选择、分层配置、四阶段进度、真实持仓和10/20/50分页订单。

账户检查是只读操作，保存成功才会持久化；密钥不回显，空白输入不代表未保存。保存账户不产生真实订单。页面的`--`表示不可用，不是资金为零。

## 交付顺序

备份与规则评估 -> 策略/底座/前端并行实现 -> 快速功能测试和小额真实验证 -> 同步发布和真实运行。不新增回测、多日paper或数据观察等待；历史成交不能代替新策略验收。

旧额度50美元本金/30美元日损可做接口验证，原版四阶段需要更高资金配置；开发不等待该问题，完整原版启动不能暗改限额。

## 本地开发

安装和构建见[README](README.md#开发验证)。在仓库根目录运行：

```powershell
python scripts/system-dashboard-server.py --host 127.0.0.1 --port 8765
```

访问 `http://127.0.0.1:8765/console/`。热更新使用 `npm --prefix web run dev`，其API代理指向8765。本地状态不代表都柏林状态。运维命令见[速查](docs/QUICK_REF.md)。

用户已授权按最新规划自主开发、调试、发布及范围内验证，无需重复询问旧授权；本轮备份和规划没有启动真实交易。
