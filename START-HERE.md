# 从这里开始

## 现在是什么

当前项目只做一件事：研究并验证 Polymarket BTC 5 分钟 Up/Down 的双边动态补仓策略。

页面地址：`https://13-115-254-211.sslip.io:8765/system-dashboard.html`

默认是纸面模拟。行情是真的，钱和订单不是。

## 最近一次账户核对

- API/公开地址：`0xA693a0E0e40BDeC3d9d4a40bD4D087A5cECFD7cd`（页面明确提示不可向此地址转账）。
- 只读页面显示现金/组合约 `$17.13`，当前无持仓。
- Relayer 签名地址：`0xDa6F73818Af63191633D8c8025508CD703780CD0`；没有 Relayer API 密钥。
- 结论：账户能登录不等于程序已经有签名权限；当前没有真实订单。

## 页面怎么用

1. 进入“交易”，先看 Polymarket 盘口、BTC 价格和数据延迟是否正常。
2. 进入“配置”，填写本次最多用多少钱、单笔多少钱、最多多少单和运行多久。
3. 回到“交易”，点击“开始模拟”。
4. 观察挂单、成交、撤单、成交额、已结算收益和当前风险。
5. 点击“停止并撤单”，再到“订单”按市场看汇总。

## 策略白话版

先在 Up 或 Down 价格合适时挂买单。哪边成交得多，就给另一边补单；只在两边合计成本不超过上限时继续补。临近市场结束、价格快速反向或资金达到上限时停止。

这不是保证每场盈利的套利。只要有一边没成交、价格突然变化、排队靠后或奖励不够，就可能亏损。

## 当前是否能用真钱

不能直接用。当前状态是 `NOT_READY_FOR_LIVE_TRADING`，原因是：

- 纸面回放无法知道真实排队位置和部分成交；
- 官方费用、返佣、奖励还不能对每笔模拟成交闭环；
- 断线恢复、订单状态和单边库存还需要真实小额试单验证。

裸 IP 会出现证书名称不匹配，请使用上面的域名地址。当前页面和采集器均部署在东京服务器；SSH 已恢复。地理接口显示 `JP / blocked=true`，但官方文档说明日本只限制网页端、API 本身可用。真钱仍保持关闭，原因是东京延迟高且真实账户订单链路尚未验收。

## 维护规则

- 只保留 BTC 5 分钟主线；天气、期权、山寨币、NegRisk 旧路线不再恢复。
- 最新结果放在 `data/`，旧中间结果不作为结论。
- 代码改动必须通过 Python 测试、TypeScript 构建和 TypeScript 测试。
- 任何实盘解锁都必须单独、明确批准。
