# 从这里开始

直接打开 **https://34-242-206-196.sslip.io/console/**。这是都柏林服务器控制台，HTTPS 使用标准 443 端口，HTTP 80 自动跳转；页面不要求登录。

当前开发目标与自主执行要求见 [自动做市交付规划 v2](docs/STRATEGY-DELIVERY-PLAN-2026-09-13.md)：保留现有前端，策略验证后提供推荐默认参数，减少逐项填写和误配，仅补实际不足。以下是现有功能说明，不表示推荐默认值或自适应能力已全部上线。

## 使用页面

1. 在总览和市场页检查数据来源、市场名称、时间戳与盘口有效性。缺报价、过期或切换中的盘口不能作为有效交易依据。
2. 在设置页填写账户，点击“检查”进行只读验证；点击“保存账户”才会持久化。检查成功不等于保存成功，检查后如需保存新密钥须重新输入。
3. 现有设置页九项接入保存：单笔金额、挂单寿命、运行模式、运行时长、累计提交金额上限、订单数上限、配对成本、决策间隔和防御撤单阈值。保存后检查版本与反馈；这些参数下次启动生效。
4. 其他高级参数仍为当前页草稿。版本化纸面启停已接线；实盘完整控制和自适应参数尚待按新规划验收。保存配置不会自行启动交易。

账户密钥不会回显；空白密钥输入不是服务端没有保存的证据。失败时查看账户区错误信息，不能用“检查通过”代替服务器“保存成功”反馈。`--` 表示暂不可用，不是余额或费用为零。

## 目前的验收边界

2026-09-10 只读核验中，V2 所需链上授权、签名匹配和私有 CLOB 查询通过；记录的可交易余额为 108.728676，仅代表那次观测。公网状态样本为 `running=false`、`mode=paper`、`live_unlocked=false`。

2026-09-11 已有受控真实下单、成交、撤单和一笔零兑付赎回证据；完整自动运行、资金账本、奖励到账、故障恢复和策略盈利仍未全部验收。paper 出现过未配对库存。当前研究主线是包含奖励的自动做市，详见 [交付状态](docs/DELIVERY.md) 和最新规划。

## 本地开发

本地服务用于开发预览，不替代公网部署。完成 [README 中的安装、测试和构建](README.md#开发验证) 后，在仓库根目录运行：

```powershell
python scripts/system-dashboard-server.py --host 127.0.0.1 --port 8765
```

打开 `http://127.0.0.1:8765/console/`。需要热更新时另开终端：

```powershell
npm --prefix web run dev
```

Vite 的 `/api` 代理指向本地 8765。本地页面读取的是本地服务配置的数据来源；必须核对来源标记，不能把本地进程状态当成都柏林状态。纸面模拟命令见 [引擎说明](_external/btc-5m-market-trading-bot/README.md)。

部署、服务名及诊断命令见 [运维速查](docs/QUICK_REF.md)。用户已授权主 Agent 按最新规划自主开发、调试、部署和范围内验证，无需重复确认旧授权；账户保存本身不产生订单或链上操作，实际启用仍需满足账户、资金与执行正确性检查。
