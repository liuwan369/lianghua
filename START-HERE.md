# 从这里开始

正式控制台：<https://34-242-206-196.sslip.io/console/>

系统当前只运行一套可配置的 **BTC 五分钟反转** 逻辑。70/75 只是参数参考，不是两套策略。实时状态、发布版本和未完成项以 [CURRENT-STATUS](docs/CURRENT-STATUS.md) 为准。

## 日常操作

1. 在“设置”检查账户连接。检查是只读操作，保存账户也不会启动交易。
2. 在“策略”填写触发价、确认价、最高买价、各阶段份额和可选预算，保存后确认版本号。
3. 价格、阶段和预算修改只影响尚未开始的新场；运行时长从下次启动生效。
4. 在“自动交易”查看当前运行、盘口、订单、成交、撤单和失败记录。
5. 在“总览”查看真实资金、当前有效持仓、账户历史、分段延迟和服务器资源。
6. 启动、暂停、恢复和停止均在控制台明确操作。停止进程后仍需核对开放订单和迟到成交。

页面中的 `--` 或“未知”表示没有可靠数据，不能理解为零。账户历史包含整个钱包活动，不等于本次策略订单。已结算且价值为零的历史持仓单独展示，不计入当前有效持仓。

## 小额真实验收

首次真实链路核对使用阶段份额 `[5,5,5,5]`，只验证 ACK、成交、撤单、重启恢复、费用、换场和结算到账。该配置不覆盖正式策略参数。真实操作由用户在控制台发起，Agent 根据产生的日志和账户结果做只读核对。

## 本地开发

```powershell
python scripts/system-dashboard-server.py --host 127.0.0.1 --port 8765 --root .
```

访问 `http://127.0.0.1:8765/console/`。本机账户、控制状态和行情来源与服务器相互独立，本机页面不能作为服务器已部署的证据。

实现、接口和验收说明见 [文档索引](docs/README.md)。
