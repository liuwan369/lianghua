# 多 Agent 开发规则

## 用户铁律

本项目开发 `Polymarket BTC 五分钟反转系统` 时，始终遵循 `C:\Users\Administrator\Desktop\话术.txt`：交付优先，避免过度工程化、流程主义、审计驱动开发、过早抽象，不用安全门槛替代交付；优先复用已经接近可用的成熟源码，先解决真实链路，再补必要文档。每个会话保持简洁，先做能运行、能验证的结果。

## 当前系统边界

- 只维护一套 BTC 五分钟反转策略。
- 不恢复旧做市、paper、回测和研究链路。
- 前端、行情、策略执行、账本/API 分目录开发，通过共享契约对接。
- 生产账户秘密不进入浏览器；真实交易状态以服务器为准。

## 协作规则

- 每个会话只修改自己负责的目录，不覆盖其他会话的修改。
- 先快速读相关代码，再做最小改动；验证范围与改动风险匹配。
- 完成时只报告修改、验证和遗留风险，不生成无关过程材料。
